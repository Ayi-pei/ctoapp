# Client-side envs (safe to expose)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Server-side envs (DO NOT prefix with NEXT_PUBLIC)
SUPABASE_URL=YOUR_SUPABASE_URL
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# External API keys (server-side only)
COINGECKO_API_KEY=
COINDESK_API_KEY=
TATUM_API_KEY=
ALPHA_VANTAGE_API_KEY=
GEMINI_API_KEY=

# 管理员初始账号（仅服务端使用）
# 切记不要加 NEXT_PUBLIC_ 前缀，这些变量只在服务端可见
# 用途：
# - ADMIN_NAME / ADMIN_KEY：管理员登录（/api/auth/login）。
# - ADMIN_AUTH：管理员的邀请码。作为下级注册的重要凭证（特别是三级代理场景）。
#   - 若服务端配置了 Supabase，管理员登录后会把 invitation_code 写入 profiles 表，
#     下级注册时会验证 invitation_code 并写入 inviter_id。
#   - 若服务端未配置 Supabase（本地开发模式），注册接口允许用 ADMIN_AUTH 作为回退逻辑。
ADMIN_NAME=
ADMIN_KEY=
ADMIN_AUTH=

# Supabase（服务端）
# 若要启用真实数据库读写，请填写：
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase（前端）可选开关（安全可公开）
# - NEXT_PUBLIC_SUPABASE_DISABLED=true   禁用前端 Supabase，避免本地网络报错
# - NEXT_PUBLIC_SUPABASE_REALTIME=false  关闭前端实时订阅
# - NEXT_PUBLIC_MARKET_DB_PERSIST=false  关闭前端 K 线持久化（仅内存模拟）
NEXT_PUBLIC_SUPABASE_DISABLED=
NEXT_PUBLIC_SUPABASE_REALTIME=
NEXT_PUBLIC_MARKET_DB_PERSIST=

# Session (server-only)
SESSION_SECRET=
SESSION_TTL=86400
